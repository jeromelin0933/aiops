global:
  scrape_interval: 15s      # 依照 PRD 規定，每 15 秒抓取一次指標
  evaluation_interval: 15s  # 評估告警規則的頻率

scrape_configs:
  - job_name: 'python-metrics'
    static_configs:
      - targets: ['host.docker.internal:8000']